<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:video="http://www.google.com/schemas/sitemap-video/1.1">

  <% @static_pages.each do |page| %>
    <url>
      <loc>https://langlets.app<%= page[:path] %></loc>
      <priority><%= page[:priority] %></priority>
      <changefreq><%= page[:changefreq] %></changefreq>
    </url>
  <% end %>

  <% @courses.each do |course| %>
    <url>
      <loc>https://langlets.app<%= course_path(course) %></loc>
      <lastmod><%= course.updated_at.iso8601 %></lastmod>
      <priority>0.8</priority>
      <changefreq>weekly</changefreq>
      <%# Needs a real cover: Google drops a video entry with no thumbnail_loc,
          and TikTok covers only exist once oEmbed has been stored. %>
      <% course_thumb = course_thumbnail_url(course) %>
      <% if course_thumb.present? && course.main_media_url.present? %>
        <video:video>
          <video:title><%= course.localized_name %></video:title>
          <video:description><![CDATA[<%= course_description(course) %>]]></video:description>
          <video:thumbnail_loc><%= course_thumb %></video:thumbnail_loc>
          <video:content_loc><%= course.main_media_url %></video:content_loc>
        </video:video>
      <% end %>
    </url>
  <% end %>

  <% @playlists.each do |lp| %>
    <url>
      <loc>https://langlets.app<%= playlist_path(lp) %></loc>
      <lastmod><%= lp.updated_at.iso8601 %></lastmod>
      <priority>0.7</priority>
      <changefreq>weekly</changefreq>
    </url>
  <% end %>
</urlset>
