<% if courses.any? %>
  <%= render layout: "shared/card_grid" do %>
    <% courses.each do |course| %>
      <%= render "course_card", course: course %>
    <% end %>
  <% end %>
<% else %>
  <p class="px-2 text-gray-400 dark:text-gray-600">No standalone clips available yet.</p>
<% end %>
