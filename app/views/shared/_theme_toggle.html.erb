<%# Light/dark theme switcher.
    By default it floats (fixed, bottom-right) and is rendered globally from the
    layout. Pass `inline: true` to drop it into a header/toolbar next to other
    buttons (e.g. the guest landing "Sign up" button).
    Wrapped in .sprout so it inherits the sprout color tokens in both themes. %>
<% inline = local_assigns.fetch(:inline, false) %>
<% content_for(:suppress_floating_theme_toggle) { "1" } if inline %>
<div class="sprout<%= ' inline-flex' if inline %>">
  <button type="button"
          class="theme-toggle<%= ' theme-toggle--inline' if inline %>"
          data-controller="theme"
          data-theme-url-value="<%= theme_preference_path %>"
          data-action="click->theme#toggle"
          aria-label="Switch between light and dark theme"
          title="Switch theme">
    <%# Moon — shown while in dark mode (tap to go light) %>
    <svg data-theme-icon="dark" <%= "hidden" unless current_theme == "dark" %>
         xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
         stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
    </svg>
    <%# Sun — shown while in light mode (tap to go dark) %>
    <svg data-theme-icon="light" <%= "hidden" unless current_theme == "light" %>
         xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
         stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="12" cy="12" r="4"></circle>
      <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"></path>
    </svg>
  </button>
</div>
