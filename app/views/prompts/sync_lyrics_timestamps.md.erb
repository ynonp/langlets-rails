Watch this video. You will receive **a portion** of an SRT file in a separate message — only the lyrics covering a specific time segment of the video. **Correct the timestamps for those lines only** so they precisely match the actual audio in that segment. Do NOT change any lyric text — only adjust the start and end times.

Use SRT standard timestamps: HH:MM:SS,mmm (hours:minutes:seconds,milliseconds) — always include hours even if 00

Provide the corrected output in "srt" format without any prefix or header.
