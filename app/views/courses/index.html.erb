<% content_for :head do %>
  <title>Langlets - Learn Languages Through Interactive Video Clips</title>
  <%= render_meta_tags(
    title: "Langlets - Learn Languages Through Interactive Video Clips",
    description: SeoHelper::DEFAULT_DESCRIPTION,
    url: canonical_url(root_path),
    image_url: nil
  ) %>
  <meta name="keywords" content="language learning, interactive video, learn English, learn Hebrew, learn French, learn Spanish, learn Arabic, bilingual education">
<% end %>

<% content_for :canonical_url, canonical_url(root_path) %>

<% content_for :structured_data do %>
  <%= render "shared/structured_data/website" %>
  <%= render "shared/structured_data/faq_page" %>
  <% if @learning_paths.any? %>
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "ItemList",
      "name": "Featured Learning Paths on Langlets",
      "itemListElement": [
        <% @learning_paths.each_with_index do |lp, idx| %>
          {
            "@type": "ListItem",
            "position": <%= idx + 1 %>,
            "url": "<%= j canonical_url(learning_path_path(lp)) %>",
            "name": "<%= j lp.name %>"
          }<%= "," unless idx == @learning_paths.size - 1 %>
        <% end %>
      ]
    }
    </script>
  <% end %>
<% end %>

<div class="sprout min-h-screen flex flex-col" style="--hue: 150;">
<!-- Top bar: Use existing authentication bar -->
<header class="py-6 border-b border-slate-700/60">
  <div class="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 py-2 flex items-center justify-between gap-4">
    <div class="flex-1">
      <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold text-slate-100 tracking-tight">
        Langlets
      </h1>
      <p class="text-slate-400 text-sm sm:text-base mt-1">Understand native content</p>
    </div>

    <% if user_signed_in? %>
      <div class="flex items-center space-x-4">
        <% daily_xp = ActivityLog.daily_xp_for_user(current_user) %>
        <% streak_info = ActivityLog.streak_info_for_user(current_user) %>
        <% streak = streak_info[:count] %>

        <div class="hidden sm:flex items-center space-x-4">
          <%= render "shared/theme_toggle" %>
          <!-- XP Display -->
          <div class="flex items-center space-x-6">
            <!-- Day Streak -->
            <div class="flex items-center text-white">
              <div class="s-streak">
                <svg width="17" height="17" viewBox="0 0 24 24" fill="none">
                  <path d="M12 2c1 3 4 4.5 4 8a4 4 0 0 1-8 0c0-1 .3-1.8.8-2.5C8 8.8 7 10.3 7 12.5a5 5 0 0 0 10 0c0-4.2-3-6.5-5-10.5z" fill="oklch(0.68 0.2 40)"/>
                  <path d="M12 9c.7 1.3 2 2 2 3.6a2 2 0 0 1-4 0c0-1 .6-1.8 2-3.6z" fill="oklch(0.82 0.15 70)"/>
                </svg>
                <%= streak %> day<%= 's' if streak != 1 %>
              </div>
            </div>

            <!-- Today's XP -->
            <div class="flex items-center">
              <div class="s-chip s-chip--active" style="--lh: 235; background: oklch(0.66 0.15 235);">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-4 h-4 mr-1">
                  <path fill-rule="evenodd" d="M10.788 3.21c.448-1.077 1.976-1.077 2.424 0l2.082 5.006 5.404.434c1.164.093 1.636 1.545.749 2.305l-4.117 3.527 1.257 5.273c.271 1.136-.964 2.033-1.96 1.425L12 18.354 7.373 21.18c-.996.608-2.231-.29-1.96-1.425l1.257-5.273-4.117-3.527c-.887-.76-.415-2.212.749-2.305l5.404-.434 2.082-5.005Z" clip-rule="evenodd" />
                </svg>
                <%= daily_xp %> XP
              </div>
            </div>
          </div>
        </div>

        <!-- Create Course Button (for authorized users) - Hidden on mobile -->
        <% if can? :create, Course %>
          <%= link_to new_course_path, class: "hidden sm:flex s-btn s-btn--sm" do %>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-4 h-4 mr-2">
              <path fill-rule="evenodd" d="M12 3.75a.75.75 0 0 1 .75.75v6.75h6.75a.75.75 0 0 1 0 1.5h-6.75v6.75a.75.75 0 0 1-1.5 0v-6.75H4.5a.75.75 0 0 1 0-1.5h6.75V4.5a.75.75 0 0 1 .75-.75Z" clip-rule="evenodd" />
            </svg>
            Create Course
          <% end %>
          <%= link_to new_import_course_path, class: "hidden sm:flex s-btn s-btn--soft s-btn--sm" do %>
            Import
          <% end %>
          <%= link_to new_resync_timestamp_path, class: "hidden sm:flex s-btn s-btn--soft s-btn--sm" do %>
            Resync
          <% end %>
        <% end %>

        <!-- User Menu -->
        <div class="relative group">
          <input type="checkbox" id="user-menu-toggle" class="hidden peer">
          <label for="user-menu-toggle" class="flex items-center cursor-pointer">
            <img src="<%= 'https://ui-avatars.com/api/?name=' + ERB::Util.url_encode(current_user.email) %>" alt="Avatar" class="w-10 h-10 rounded-full shadow" style="border: 2px solid var(--accent);" />
          </label>
          <div class="absolute right-0 mt-2 w-56 rounded-2xl shadow-lg opacity-0 invisible peer-checked:opacity-100 peer-checked:visible transition-all duration-150 z-50 overflow-hidden" style="background: var(--surface); border: 1px solid var(--border);">
            <% if native_app? %>
              <% current_lang = Language.find_by(iso_name: params[:lang]) %>
              <%= link_to onboarding_language_path(returnto: request.fullpath), class: "block px-4 py-3 s-body hover:opacity-70", style: "color: var(--text);" do %>
                <span class="text-sm">Learning Language: <%= current_lang&.native_name || "Select" %></span>
              <% end %>
              <div style="border-top: 1px solid var(--border);"></div>
            <% end %>
            <div class="sm:hidden px-4 py-3 flex items-center justify-between mobile-header-theme" style="color: var(--text);">
              <span class="text-sm">Theme</span>
              <%= render "shared/theme_toggle" %>
            </div>
            <div class="sm:hidden px-4 pb-3 flex items-center gap-3 mobile-header-stats">
              <div class="s-streak">
                <svg width="17" height="17" viewBox="0 0 24 24" fill="none">
                  <path d="M12 2c1 3 4 4.5 4 8a4 4 0 0 1-8 0c0-1 .3-1.8.8-2.5C8 8.8 7 10.3 7 12.5a5 5 0 0 0 10 0c0-4.2-3-6.5-5-10.5z" fill="oklch(0.68 0.2 40)"/>
                  <path d="M12 9c.7 1.3 2 2 2 3.6a2 2 0 0 1-4 0c0-1 .6-1.8 2-3.6z" fill="oklch(0.82 0.15 70)"/>
                </svg>
                <%= streak %> day<%= 's' if streak != 1 %>
              </div>
              <div class="s-chip s-chip--active" style="--lh: 235; background: oklch(0.66 0.15 235);">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-4 h-4 mr-1">
                  <path fill-rule="evenodd" d="M10.788 3.21c.448-1.077 1.976-1.077 2.424 0l2.082 5.006 5.404.434c1.164.093 1.636 1.545.749 2.305l-4.117 3.527 1.257 5.273c.271 1.136-.964 2.033-1.96 1.425L12 18.354 7.373 21.18c-.996.608-2.231-.29-1.96-1.425l1.257-5.273-4.117-3.527c-.887-.76-.415-2.212.749-2.305l5.404-.434 2.082-5.005Z" clip-rule="evenodd" />
                </svg>
                <%= daily_xp %> XP
              </div>
            </div>
            <div class="sm:hidden" style="border-top: 1px solid var(--border);"></div>
            <!-- Create Course Button (for mobile) -->
            <% if can? :create, Course %>
              <%= link_to "Create Course", new_course_path, class: "sm:hidden block px-4 py-3 s-body hover:opacity-70", style: "color: var(--accent);" %>
              <%= link_to "Import Courses", new_import_course_path, class: "sm:hidden block px-4 py-3 s-body hover:opacity-70", style: "color: var(--accent);" %>
              <%= link_to "Resync Timestamps", new_resync_timestamp_path, class: "sm:hidden block px-4 py-3 s-body hover:opacity-70", style: "color: var(--accent);" %>
              <div class="sm:hidden" style="border-top: 1px solid var(--border);"></div>
            <% end %>
            <% current_user.languages_with_saved_words.order(:iso_name).each do |language| %>
              <%= button_to "📚 Review Words (#{language.iso_name})", review_lessons_path(language_code: language.iso_name),
                method: :post,
                class: "w-full text-left px-4 py-3 s-body hover:opacity-70",
                style: "color: var(--text);",
                data: { turbo: false }
              %>
            <% end %>
            <% if current_user.languages_with_saved_words.exists? %>
              <div style="border-top: 1px solid var(--border);"></div>
            <% end %>
            <%= link_to "Logout",
              destroy_user_session_path(returnto: request.fullpath),
              method: :delete,
              data: { turbo_method: :delete, controller: "bridge--sign-out", action: "click->bridge--sign-out#notifySignOut" },
              class: "block px-4 py-3 s-body hover:opacity-70",
              style: "color: var(--text);"
            %>
          </div>
        </div>
      </div>
    <% else %>
      <div class="flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-3">
        <span class="text-slate-300 text-sm sm:text-base hidden sm:block">Sign in now to not lose your progress</span>
        <div class="flex items-center gap-2">
          <%= render "shared/theme_toggle" %>
          <%= link_to "Sign up", new_user_registration_path(returnto: request.fullpath), class: "s-btn s-btn--sm" %>
        </div>
      </div>
    <% end %>
  </div>
</header>

<main class="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 py-8 overflow-x-hidden w-full flex-1">

  <!-- Flash Messages -->
  <% if notice %>
    <div class="s-card mb-6" style="padding: 14px 16px; color: var(--accent);">
      <%= notice %>
    </div>
  <% end %>

  <% if alert %>
    <div class="s-card mb-6" style="padding: 14px 16px; color: oklch(0.6 0.2 25);">
      <%= alert %>
    </div>
  <% end %>

  <!-- GEO: Entity-rich description for search engines and AI crawlers -->
  <section class="mb-8 px-2">
    <p class="text-slate-300 text-lg leading-relaxed">
      Understand native content in
      <strong class="text-white">English</strong>,
      <strong class="text-white">Hebrew</strong>,
      <strong class="text-white">French</strong>,
      <strong class="text-white">Spanish</strong>, and
      <strong class="text-white">Arabic</strong>.
      Each course breaks down bilingual phrases with word-level translations, timestamps,
      and AI-powered activities designed to build your listening, speaking, and
      comprehension skills through spaced repetition.
    </p>
  </section>

  <!-- Continue Learning Section - show courses user has started -->
  <% if user_signed_in? && @continue_learning_courses.any? %>
    <section class="mb-10">
      <h2 class="text-3xl font-bold text-slate-100 mb-6 px-2">Continue Learning</h2>
      <div class="swiper overflow-hidden" data-controller="swiper" data-swiper-target="container">
        <div class="swiper-wrapper">
          <% @continue_learning_courses.each do |course| %>
            <div class="swiper-slide">
              <%= render 'course_card', course: course, container_classes: "block w-64 sm:w-72 md:w-80 s-card cursor-pointer flex flex-col h-full" %>
            </div>
          <% end %>
        </div>
      </div>
    </section>
  <% end %>

  <!-- Courses (Learning Paths) -->
  <% if @learning_paths.any? %>
    <section class="mb-10">
      <h2 class="text-3xl font-bold text-slate-100 mb-6 px-2">Courses</h2>
      <div class="swiper overflow-hidden" data-controller="swiper" data-swiper-target="container">
        <div class="swiper-wrapper">
          <% @learning_paths.each_with_index do |learning_path, index| %>
            <% lp_hue = lang_hue(learning_path.courses.first&.language&.iso_name) %>
            <div class="swiper-slide">
              <%= link_to learning_path_path(learning_path), class: "block w-64 sm:w-72 md:w-80 s-card cursor-pointer flex flex-col h-full overflow-hidden", style: "padding: 0;" do %>
                <!-- Path Image -->
                <% if learning_path.cover_image_url %>
                  <img src="<%= learning_path.cover_image_url %>" alt="<%= learning_path.name %>" class="w-full h-40 object-cover flex-shrink-0">
                <% else %>
                  <div class="s-thumb w-full h-40 flex-shrink-0" style="--th: <%= lp_hue %>; border-radius: 0;"></div>
                <% end %>

                <!-- Path Content -->
                <div class="p-4 flex flex-col flex-grow">
                  <div class="flex-grow">
                    <h3 class="s-h2 mb-2"><%= learning_path.name %></h3>
                    <% if learning_path.description.present? %>
                      <p class="s-small mb-3 line-clamp-2"><%= truncate(learning_path.description, length: 80) %></p>
                    <% end %>
                    <div class="s-small mb-3">
                      <%= pluralize(learning_path.courses.count, 'clip') %>
                    </div>
                  </div>
                  <div class="s-btn w-full">Start Path</div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </section>
  <% end %>

  <!-- Standalone Clips -->
  <% if @all_courses.any? %>
    <section class="px-2">
      <h2 class="text-3xl font-bold text-slate-100 mb-6 px-2">Standalone Clips</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 sm:gap-6 md:gap-8">
        <% @all_courses.each do |course| %>
          <%= render 'course_card', course: course %>
        <% end %>
      </div>
    </section>
  <% end %>

  <!-- Row: "Next For You" - only show if user signed in and has recommended courses -->
  <% if user_signed_in? && @recommended_courses.any? %>
    <section class="mb-10 mt-10">
      <h2 class="text-3xl font-bold text-slate-100 mb-6 px-2">Next For You</h2>
      <div class="swiper overflow-hidden" data-controller="swiper" data-swiper-target="container">
        <div class="swiper-wrapper">
          <% @recommended_courses.each do |course| %>
            <div class="swiper-slide">
              <%= render 'course_card', course: course, container_classes: "block w-64 sm:w-72 md:w-80 s-card cursor-pointer flex flex-col h-full" %>
            </div>
          <% end %>
        </div>
      </div>
    </section>
  <% end %>

</main>

<footer class="border-t mt-auto" style="background: var(--surface); border-color: var(--border);">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
    <div class="flex flex-col sm:flex-row justify-between items-center space-y-4 sm:space-y-0">
      <div class="s-small">
        © <%= Date.current.year %> Langlets. All rights reserved.
      </div>
      <div class="flex space-x-6">
        <%= link_to "Privacy Policy", "/home/privacy", class: "s-small hover:opacity-70" %>
        <%= link_to "Terms of Service", "/home/terms", class: "s-small hover:opacity-70" %>
      </div>
    </div>
  </div>
</footer>
</div>
