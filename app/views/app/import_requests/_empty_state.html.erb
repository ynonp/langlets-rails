<%# Nothing typed yet. One line, and that's the whole empty state: this sheet
    does one thing, and the hint under the input already says which links work. %>
<p class="text-center text-app-text-3 text-[13px] leading-relaxed py-7 px-6">
  <%= t("app.import_requests.new.get_started") %>
</p>
