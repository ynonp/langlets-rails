<div class="flex flex-col min-h-screen w-full bg-gray-900">
<%= render partial: 'activity_navigation' %>
<div class="flex-1 mt-4 bg-gray-800">
  <%= render partial: @activity.partial_name, locals: { params: @activity.activity_params } %>
</div>
</div>
