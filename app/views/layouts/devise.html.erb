  <turbo-frame id="devise">
    <dialog
      open
      class="fixed inset-0 w-screen h-screen backdrop:bg-black backdrop:bg-opacity-75 backdrop:backdrop-blur-sm">
      <%= yield %>
    </dialog>    
  </turbo-frame>