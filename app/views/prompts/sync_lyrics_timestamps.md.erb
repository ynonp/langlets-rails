Watch this video. You will receive an SRT file with approximate timestamps in a separate message. **Correct all timestamps** so they precisely match the actual audio. Do NOT change any lyric text — only adjust the start and end times.

Use SRT standard timestamps: MM:SS,mmm (minutes:seconds,milliseconds)

Provide the corrected output in "srt" format without any prefix or header.
